<!-- create a dashboard to see all the "available" profiles -->
<!-- filter by rate, interest, day_rate range -->
<!-- order by date_created, day_rate, avg_review_rating -->
<!-- display the number of reviews for each profile -->
<!-- display username, iterest, day_rate, number of reviews, created_at -->
<!-- cl_image_path(friend_profile.photo.key, height: 300, width: 400, crop: :fill) -->

<div class="container">
  <div class="col-md-12 text-center">
    <h1 class="title">Friend Profiles</h1>
  </div>
  <div class="card-container">
    <% @friend_profiles.each do |friend_profile| %>
      <%= link_to friend_profile_path(friend_profile) do %>
      <div class="card mb-3">
      <img src="..." class="card-img-top" alt="...">
      <div class="card-body">
      <h2 class="h2-test"><%= friend_profile.username %></h2>
          <p class="card-text"> Interest: <%= friend_profile.interest %></p>
          <p class="card-text"> Price per day: <%= friend_profile.day_rate.round(2) %> €</p>
          <p class="card-text"> Date created: <%= friend_profile.created_at.strftime("%d/%m/%Y") %></p>
          <p class="card-text"> Times booked: <%= friend_profile.bookings.count %></p>
        </div>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
